<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <div class="container">

      <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
      <% if signed_in? %>
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <% end %>

       <!-- Be sure to leave the brand out there if you want it shown -->
      <a class="brand" href=<%= root_path %>> <%= image_tag "titlewhite.png" %></a>

      <!-- Everything you want hidden at 940px or less, place within here -->
      <% if signed_in? %>
      <div class="nav-collapse">
        <% if !controller.to_s.start_with?("#<Devise") %>
          <ul class="nav">
            <li class= <%="active" if current_page?(root_path)%> >
              <a href="/" class="nav-view-text">WEEK</a>
            </li>
            <li class= <%="active" if controller.controller_name == 'runs' && controller.action_name == 'summary' %> >
              <a href="/summary" class="nav-view-text">SUMMARY</a>
            </li>
          </ul>
        <% end %>
        <ul class="nav pull-right">
          <li class="dropdown">
            <a class="dropdown-toggle nav-account-text"
            data-toggle="dropdown"
            href="#">
              ACCOUNT
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to current_user.email, :controller => "devise/registrations", :action => "edit" %></li>
              <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
