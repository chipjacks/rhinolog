<!DOCTYPE html>
<html class=<%= 'fullpage' if !signed_in? && current_page?(root_path) %>>
<head>
  <title><%= full_title(yield :title) %></title>
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag %>
  <link rel='image_src' type='image/png' src=http://www.rhinolog.net/images/icon.png>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script type="text/javascript">
    var _gaq = _gaq || [];
    _gaq.push(['_setAccount', 'UA-35249110-1']);
    _gaq.push(['_trackPageview']);

    (function() {
      var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
      ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
      var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
    })();
  </script>
</head>
<body class=<%= 'fullpage' if !signed_in? && current_page?(root_path) %>>
    <%= render 'layouts/header' %>
    <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <%= value.html_safe %>
        </div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <!-- <br> -->
      <!-- <%= debug(params) if Rails.env.development? %> -->
    </div>
</body>
</html>
